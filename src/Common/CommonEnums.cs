namespace Common
{
    #region DateFormat

    /// <summary>
    /// Enum to get several date format.
    /// </summary>
    public enum DateFormat
    {
        Undefined = -1,
        DateDebug,
        DateFile,
        DateLong,
        Date,
    }

    #endregion DateFormat
}
